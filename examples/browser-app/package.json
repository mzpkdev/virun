{
  "name": "browser-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "serve": "virun serve --entry index.html --target browser --port 3000",
    "build": "virun build --entry index.html --target browser --outdir dist",
    "clean": "virun clean",
    "test": "virun test",
    "typecheck": "virun typecheck"
  },
  "dependencies": {},
  "devDependencies": {
    "virun": "file:../.."
  }
}
