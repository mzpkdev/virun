{
  "name": "browser-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "virun serve --target browser --port 3000",
    "build": "virun build --target browser --outdir dist",
    "clean": "virun clean",
    "test": "virun test"
  },
  "dependencies": {},
  "devDependencies": {
    "virun": "file:../.."
  }
}
