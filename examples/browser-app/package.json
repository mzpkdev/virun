{
  "name": "browser-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "virun serve --target browser --entry index.html --port 3000",
    "build": "virun build --target browser --entry index.html --outdir dist",
    "clean": "virun clean --outdir dist",
    "test": "virun test"
  },
  "dependencies": {},
  "devDependencies": {
    "virun": "file:../.."
  }
}

